# Daily report — 2026-08-25 (second run)

## Ledger changes today

- **New SEED trend**: `agentic-injection-001` — Agentic prompt-injection & agent-subsystem
  attacks (tools, skills, memory). Promoted from `observation_queue`: cleared the trend bar
  with 4 new independent primaries in one run — InjecMEM (memory injection), SkillBloat
  (skill injection / token amplification), AgentFlow and AEGIS (defensive frameworks) —
  on top of the already-verified MCP threat-model paper (arXiv 2603.22489).
- `local-inference-001`: evidence backfilled from yesterday's verified advisories (Ollama
  CVE-2026-7482, llama.cpp CVE-2026-27940 lines added to the trend block); no new
  inference-server CVEs today. Ollama v0.33.0-rc3 noted (2026-08-21).
- `observation_queue`: −1 promoted (MCP cluster), +3 (Hydra CVE-2026-68508 verified via
  GHSA; OWASP GenAI LLM Top 10 2026 page verified; vendor MCP writeups re-queued) → 8 live.
- `study_shelf`: +2 — InjecMEM, llama.cpp v0.3.0.
- README regenerated from ledger in the same commit.

## Top 3 of the day

1. [InjecMEM — Memory Injection Attack on LLM Agent Memory Systems](https://arxiv.org/abs/2608.23471) — arXiv (2026-08-24)
   Single interaction, no read/edit access to the memory store, steers later
   retrieval-conditioned answers across multiple memory systems and backbones. Persistent
   agent memory is now an injection surface.

2. [SkillBloat — Token Amplification via Skill Injection in LLM Coding Agents](https://arxiv.org/abs/2608.21929) — arXiv (2026-08-22)
   Malicious agent "skills" as a trusted instruction channel; 5.4–10.1x token amplification
   — an economic resource-abuse vector orthogonal to classic skill poisoning.

3. [llama.cpp v0.3.0](https://github.com/ggml-org/llama.cpp/releases/tag/v0.3.0) — ggml-org (2026-08-25)
   First v0.3.x tag: dots3-note multimodal (new DSA-ISWA KV cache), MTP for GLM-4.5-Air,
   DeepSeek 4 tensor-split and multi-sequence rollback fixes, ggml v0.22.0.

## Study picks

- InjecMEM — see above. Agent-memory hardening is now a red-team checklist item.
- llama.cpp v0.3.0 — see above. Re-baseline local runtime testing.

## Operator notification (curator must read)

- **State persistence is broken**: this session started with an empty sandbox — the repo at
  `/mnt/agents/output/ai-ml-redteam-radar` did not exist; run recovered by cloning the
  GitHub remote during a curator-granted public window. AGENTS.md assumes the repo persists
  across scheduled runs — it did not today. Once the repo is private again, a scheduled run
  that finds an empty sandbox CANNOT self-heal (no credentials survive sessions). Fix
  options: keep the clone in the persistent mount, store a read-only credential where
  sessions can reach it, or keep a public mirror. Logged in `TRENDS.md#blockers`.

## Persist status

- Local commit `radar: daily update 2026-08-25` created. Push to `origin` (GitHub)
  attempted twice per Git conventions and FAILED — push requires credentials and none
  persist in this environment. The commit exists only in this sandbox until it is pushed
  from a session with access.

## Next

- Open Microsoft "State of MCP Security 2026" + the Aptible PR-title-hijack writeup to raise
  `agentic-injection-001` confidence toward `emerging`.
- Open the OWASP GenAI LLM Top 10 2026 final document and diff vs v1.1 (taxonomies axis).
- Open NVIDIA Triton PSIRT records (CVE-2026-24207/-24209/-24210/-24215); likely fold into
  `local-inference-001`.
- Verify the malicious-HuggingFace-models lead (hivesecurity writeup + CVE-2026-6859).
- Re-test r/LocalLLaMA (degraded 2 consecutive runs).
- arXiv watch: SecOPD (2608.21500), "Breaking the Assumptions" (2608.21895) — open if the
  injection-defense cluster keeps growing.
